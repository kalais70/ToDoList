@page
@model ToDoList.IndexModel



<br />
<div class="container row p-0 m-0">
    <div class="col-9">
        <h2 class="text-info">ToDO List</h2>
    </div>
    <div class="col-3">
        <a asp-page="Create" class="btn btn-info form-control text-white">Create New Task</a>
    </div>
    <br /><br />
    <br />
    @*<div class="col-12 border p-3 mt-3">
            <form method="post">
                @if (Model.ToDos.Count() > 0)
                {
                    <table class="table table-striped border">
                        <tr class="table-secondary">
                            <th>
                                <label asp-for="ToDos.FirstOrDefault().Name"></label>
                            </th>
                            <th>
                                <label asp-for="ToDos.FirstOrDefault().Task"></label>
                            </th>
                            <th>
                                <label asp-for="ToDos.FirstOrDefault().SubTask"></label>
                            </th>
                            <th>
                                <label asp-for="ToDos.FirstOrDefault().Status"></label>
                            </th>
                            <th>
                                <label asp-for="ToDos.FirstOrDefault().Remarks"></label>
                            </th>
                             <th>
                                <label asp-for="ToDos.FirstOrDefault().CreatedDate"></label>
                            </th>
                             <th>
                                <label asp-for="ToDos.FirstOrDefault().UpdatedDate"></label>
                            </th>
                        </tr>
                        @foreach (var item in Model.ToDos)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(m => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(m => item.Task)
                                </td>
                                 <td>
                                    @Html.DisplayFor(m => item.SubTask)
                                </td>
                                <td>
                                    @Html.DisplayFor(m => item.Status)
                                </td>
                                 <td>
                                    @Html.DisplayFor(m => item.Remarks)
                                </td>
                                 <td>
                                    @Html.DisplayFor(m => item.CreatedDate)
                                </td>
                                 <td>
                                    @Html.DisplayFor(m => item.UpdatedDate)
                                </td>
                                <td>
                                    <button asp-page-handler="Delete" asp-route-id="@item.Id " onclick="return confirm('Are you sure you want to delete?')" class="btn btn-danger btn-sm">Delete</button>
                                    <a asp-page="Edit" asp-route-Id="@item.Id" class="btn btn-success btn-sm text-white">Edit</a>
                                </td>
                            </tr>
                        }
                    </table>

                }
                else
                {
                    <p>No Task Assigned</p>
                }
            </form>
        </div>
        <div class="col-12" style="text-align:center">
            <br />
            <span class="h3 text-info">OR</span>
            <br /><br />
        </div>*@
    <div class="col-12" border p-3">
        <table id="DT_load" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Task</th>
                    <th>SubTask</th>
                    <th>Status</th>
                    <th>Remarks</th>
                    <th>CreatedDate</th>
                    <th>UpdatedDate</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
</div>
@section Scripts{ 
<script src="~/js/ToDoList.js"></script>
}
